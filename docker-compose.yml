version: '3.8'

services:
  minishell_new:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: minishell_new_container
    # Run in interactive mode with TTY
    stdin_open: true
    tty: true
    # Mount the project directory for development
    volumes:
      - .:/minishell_new
    # Override entrypoint for development mode
    # This allows you to work on the code and rebuild
    entrypoint: /bin/bash
    working_dir: /minishell_new